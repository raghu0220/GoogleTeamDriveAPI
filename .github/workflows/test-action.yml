name: build mule project
on: workflow_dispatch
jobs:
  build code:
    runs-on: ubuntu-latest
    steps:
      - name: print greeting
      run: echo "hello world"
      - name: print good bye
      run: echo "Done"
